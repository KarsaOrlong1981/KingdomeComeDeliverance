<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:KingdomeComeDeliverance.ViewModels"
             xmlns:controls="clr-namespace:KingdomeComeDeliverance.Controls"
             x:DataType="vm:MapsPageViewModel"
             x:Class="KingdomeComeDeliverance.Pages.MapsPage"
             BindingContext="{Binding Source={StaticResource Locator}, Path=Maps}"
             Title="MapsPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <controls:NavigationBar />
        <Image Grid.Row="1" Source="{Binding Source={StaticResource KCD_6_BackgroundImagePath}}" Opacity="0.6" Aspect="AspectFill"  Margin="0,0,0,0"/>
    </Grid>
</ContentPage>